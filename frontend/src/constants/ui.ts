/**
 * UI-related constants for consistent behavior across components
 */

// Scroll behavior constants
export const SCROLL_THRESHOLD_PX = 100; // Distance from bottom to consider "at bottom"
export const SCROLL_DEBOUNCE_MS = 150; // Debounce delay for scroll events

// Text preview lengths for event timeline
export const MAX_REASONING_PREVIEW_LENGTH = 100; // Max characters for reasoning preview
export const MAX_REFLECTION_PREVIEW_LENGTH = 80; // Max characters for reflection reasoning

// Animation durations
export const SMOOTH_SCROLL_DURATION_MS = 300; // Duration for smooth scroll animations
